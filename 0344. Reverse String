class Solution {
    public void reverseString(char[] s) {
        int p = 0;
        int q = s.length - 1;
        
        while (p<q){
            char tmp = s[p];
            s[p]=s[q];
            s[q]=tmp;
            p++; 
            q--;
        }
    }
}
